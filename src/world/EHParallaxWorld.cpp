#include "EHParallaxWorld.h"

// void EHParallaxWorld::LateTick(float) {
//     FVector cameraPosition = FVector::Zero;
//
//     for (const FBackgroundLayer& layer : backgroundLayers) {
//         float temp = cameraPosition.x * (1 - layer.parallaxEffectStrength);
//         float dist = cameraPosition.x * layer.parallaxEffectStrength;
//
//     }
// }
